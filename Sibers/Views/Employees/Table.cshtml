@model List<EmployeeViewModel>
@{
    ViewData["Title"] = "Сотрудники";
}

<div>
    <h1 class="table-title">Сотрудники</h1>
    <div class="tends-to-center" id="table-wrapper">
        <div id="table-scroll">
            <table class="table-content">
                <thead id="table-head">
                    <tr class="sticky white-oswald blue-background">
                        <th>Фамилия</th>
                        <th>Имя</th>
                        <th>Отчество</th>
                        <th>Почта</th>
                        <th>Компания</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null)
                    {
                        @foreach (var employee in Model)
                        {
                            <tr>
                                <td>
                                    <a class="table-text" asp-controller="CustomerCompany" asp-action="ChangeRow" asp-route-id="@employee.Id">
                                        @employee.LastName
                                    </a>
                                </td>
                                <td>@employee.LastName</td>
                                <td>@employee.LastName</td>
                                <td>@employee.MiddleName</td>
                                <td>@employee.Email</td>
                                <td>@employee.Company</td>
                            </tr>
                        }
                    }
                </tbody>
                <tfoot>
                    <tr>
                        <td class="last-row">
                            <a class="table-text" asp-controller="Employees" asp-action="ChangeRow">Создать новую запись</a>
                        </td>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div>
